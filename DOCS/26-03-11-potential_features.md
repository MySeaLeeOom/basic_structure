## Check issues in the repository

## Secrest: Why passwords in secrets?

Commands like ps aux or docker inspect can expose env variables. They are also broadcast to sidecar containers. Secrets remain inside. 

### URL for a specific note to be shared/edited

How to save a note with adifferent access permissions

Make a tables for a specific URL (created/generated for a specific note)

Make a table where users can be removed and added. Admin is the owner (can be passed to another?)

--------
URL:
notes_id:
admin_user_id:
view_permissions:
edit_permissions:

--------
