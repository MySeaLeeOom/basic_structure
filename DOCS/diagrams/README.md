# Diagrams

This folder contains deployment diagrams for the project.

- `container-deployment.md` — topology flowchart and request sequence diagram (Mermaid). 

Tips:
- Open the `.md` in VS Code and use a Mermaid-capable Markdown preview, or paste the Mermaid blocks into https://mermaid.live/ to render.
