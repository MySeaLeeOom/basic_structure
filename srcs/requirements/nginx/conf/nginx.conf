server {
    listen 80;

    # API routes (specific)
    location /api/notes {
        proxy_pass http://notes:3000;
    }

    location /api/auth {
        proxy_pass http://auth:3001;
    }

    # Frontend catch-all
    location / {
        proxy_pass http://frontend:5173;
    }
}
