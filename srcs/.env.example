DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=transcendence

AUTH_DB_USER=auth_user
# AUTH_DB_PASSWORD=password //now in secrets, chosen by us
AUTH_DB_NAME=auth_db

GITHUB_CLIENT_ID="<generated by github>"
# GITHUB_CLIENT_SECRET="123" //now in secrets, generated by github
# SESSION_SECRET_KEY="123"  //now in secrets, randomly chosen

OIDC_CLIENT_ID=""
OIDC_CLIENT_SECRET=""

# Service Ports & Hostnames (Internal Network)
AUTH_PORT="3000"
AUTH_ADDR="auth:${AUTH_PORT}"
FRONTEND_PORT="3000"
FRONTEND_ADDR="frontend:${FRONTEND_PORT}"
NOTES_PORT="3003"
NOTES_ADDR="notes:${NOTES_PORT}"
POSTGRES_PORT="5432"
POSTGRES_ADDR="postgres:${POSTGRES_PORT}"
