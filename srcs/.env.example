DB_USER=postgres
DB_PASSWORD=your_password_here 
DB_NAME=transcendence

AUTH_DB_USER=auth_user
AUTH_DB_NAME=auth_db

HOST_IP=the server ip

# this needs a setup with the github app that does auth
GITHUB_CLIENT_ID="Ov23liL3n5DgYL8mKbwi"
GITHUB_CALLBACK_URL="https://localhost:8443/api/auth/login/github/callback" # needs to match the github app
WEBSITE_URL="https://localhost:8443"

# AUTH_DB_PASSWORD=password //now in secrets, chosen by us
# GITHUB_CLIENT_SECRET="123" //now in secret, generated in github
# SESSION_SECRET_KEY="123"  #now in secrets, randomly chosen, only used for gateway now

LLM_BASE_URL=http://${HOST_IP}:11434/v1

# Service Ports & Hostnames (Internal Network)
AUTH_PORT="3000"
AUTH_ADDR="auth:${AUTH_PORT}"
FRONTEND_PORT="3000"
FRONTEND_ADDR="frontend:${FRONTEND_PORT}"
NOTES_PORT="3003"
NOTES_ADDR="notes:${NOTES_PORT}"
POSTGRES_PORT="5432"
POSTGRES_ADDR="postgres:${POSTGRES_PORT}"
UID=101819
GID=4223
